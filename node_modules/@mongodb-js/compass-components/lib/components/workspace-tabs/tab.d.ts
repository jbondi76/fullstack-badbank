import React from 'react';
import type { glyphs } from '@leafygreen-ui/icon';
export type TabTheme = {
    '--workspace-tab-background-color': string;
    '--workspace-tab-selected-background-color': string;
    '--workspace-tab-border-color': string;
    '--workspace-tab-color': string;
    '--workspace-tab-selected-color': string;
    '&:focus-visible': {
        '--workspace-tab-selected-color': string;
        '--workspace-tab-border-color': string;
    };
};
type IconGlyph = Extract<keyof typeof glyphs, string>;
type TabProps = {
    title: string;
    isSelected: boolean;
    isDragging: boolean;
    onSelect: () => void;
    onClose: () => void;
    iconGlyph: IconGlyph | 'Logo';
    tabContentId: string;
    subtitle?: string;
    tabTheme?: TabTheme;
};
declare function Tab({ title, isSelected, isDragging, onSelect, onClose, tabContentId, iconGlyph, subtitle, tabTheme, ...props }: TabProps & React.HTMLProps<HTMLDivElement>): JSX.Element;
export { Tab };
//# sourceMappingURL=tab.d.ts.map
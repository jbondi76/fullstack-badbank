type GuideCueData = Array<{
    groupId?: string;
    cueId: string;
}>;
export interface GuideCueStorage {
    isCueVisited: (cueId: string, groupId?: string) => boolean;
    markCueAsVisited: (cueId: string, groupId?: string) => void;
}
export declare class CompassGuideCueStorage implements GuideCueStorage {
    private readonly storage;
    private readonly key;
    constructor(storage?: Pick<Storage, 'getItem' | 'setItem'>, key?: string);
    get data(): GuideCueData;
    set data(newData: GuideCueData);
    isCueVisited(cueId: string, groupId?: string): boolean;
    markCueAsVisited(cueId: string, groupId?: string): void;
}
export {};
//# sourceMappingURL=guide-cue-storage.d.ts.map
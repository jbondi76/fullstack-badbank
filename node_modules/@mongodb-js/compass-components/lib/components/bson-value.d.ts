import React from 'react';
import type { TypeCastMap } from 'hadron-type-checker';
import type { DBRef } from 'bson';
type ValueProps = {
    [type in keyof TypeCastMap]: {
        type: type;
        value: TypeCastMap[type];
    };
}[keyof TypeCastMap] | {
    type: 'DBRef';
    value: DBRef;
};
type ValueTypes = ValueProps['type'];
export declare const BSONValueContainer: React.FunctionComponent<React.HTMLProps<HTMLDivElement> & {
    type?: ValueTypes;
    chidren?: React.ReactChildren;
}>;
declare const BSONValue: React.FunctionComponent<ValueProps>;
export default BSONValue;
//# sourceMappingURL=bson-value.d.ts.map
import React from 'react';
import { Theme } from '@leafygreen-ui/lib';
import { GuideCueProps } from './types';
type TooltipContentProps = Partial<GuideCueProps> & {
    theme: Theme;
    title: string;
    isStandalone: boolean;
    buttonText: string;
    onEscClose: () => void;
    handleButtonClick: () => void;
    handleCloseClick: () => void;
    usePortal?: boolean;
};
declare function TooltipContent({ darkMode, open, setOpen, tooltipJustify, tooltipAlign, refEl, portalClassName, portalContainer, scrollContainer, popoverZIndex, theme, title, isStandalone, buttonText, numberOfSteps, currentStep, children, tooltipClassName, onEscClose, handleButtonClick, handleCloseClick, usePortal, ...tooltipProps }: TooltipContentProps): React.JSX.Element;
declare namespace TooltipContent {
    var displayName: string;
}
export default TooltipContent;
//# sourceMappingURL=TooltipContent.d.ts.map
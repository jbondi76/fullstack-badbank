export interface Person {
    id: string;
    firstName: string;
    lastName: string;
    age: number;
    visits: number;
    status: 'relationship' | 'complicated' | 'single';
    subRows?: Array<Person>;
    index?: number;
}
/**
 * Generates a series of sample data to be used in Storybook
 * @param renderingExpandableRows indicates whether the `renderExpandedContent` prop should be defined in some rows
 * @param lens number of rows in each level of nesting
 * @returns Array<Person>
 */
export declare function makeData(renderingExpandableRows: boolean, ...lens: Array<number>): Person[];
export declare const makeKitchenSinkData: (count: number) => object[];
//# sourceMappingURL=makeData.testutils.d.ts.map
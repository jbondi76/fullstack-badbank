import React from 'react';
import { ToastStack } from '../ToastContext';
export declare const toastPortalClassName: string;
interface ToastContainerProps {
    stack: ToastStack;
    portalClassName?: string;
}
/**
 * ToastContainer is responsible for rendering the stack of toasts provided.
 * Rendered by the `ToastProvider`
 *
 * @internal
 */
export declare const ToastContainer: ({ stack, portalClassName, }: ToastContainerProps) => React.JSX.Element;
export {};
//# sourceMappingURL=ToastContainer.d.ts.map